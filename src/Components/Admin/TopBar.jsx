import { Navbar, Container, Button } from 'react-bootstrap';

const TopBar = () => {
  return (
    <Navbar  expand="lg" className="shadow-sm mb-3 bg-success  text-white">
      <Container fluid>
        <Navbar.Brand className='p-4'>
        
        </Navbar.Brand>

      </Container>
    </Navbar>
  );
};

export default TopBar;
